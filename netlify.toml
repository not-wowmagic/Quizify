[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

# Required for Next.js Server Actions and SSR on Netlify
[[plugins]]
  package = "@netlify/plugin-nextjs"
