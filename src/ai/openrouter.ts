// Deprecated compatibility wrapper. Redirects existing imports to the
// OpenAI-compatible helper while the codebase transitions away from the old
// name.
export { callOpenAI as callOpenRouter } from './openai';
